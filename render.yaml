services:
  - type: web
    name: choirx-audiblefix-api
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGINS
        value: "*"
      - key: MAX_UPLOAD_MB
        value: "12"
      - key: MAX_AUDIO_SECONDS
        value: "90"
