services:
  - type: web
    name: choirx-lite-api
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGINS
        value: "*"
      - key: MAX_UPLOAD_MB
        value: "8"
      - key: MAX_AUDIO_SECONDS
        value: "75"
